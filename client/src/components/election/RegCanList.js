import React from 'react';

const RegCanList = (props) => {
  
  return (
    <>
      <h1>{props.arr[0]}</h1>
      {/* <ul>
          {candidateInfo.map(data=>(
              <li key={data.id}>{data.position + " | " + data.name}</li>
          ))}
      </ul> */}
    </>
  )
}

export default RegCanList